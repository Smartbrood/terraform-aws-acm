formatter: markdown table
# header-from: ../docs/header.md
output:
  file: "./README.md"
  mode: replace
  template: |-
    # terraform-aws-acm

    Terraform module to create ACM Certificate

    {{ .Content }}

sections:
  hide-all: true
  show:
    - header
    - inputs
    - outputs
    - providers
    - resources

sort:
  enabled: true
  by: required